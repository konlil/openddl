#include "detail.h"

bool openddl::detail::semantic_check(std::vector<Command> & commands, std::vector<Error> & errors, std::map<std::string, int> & global_names, std::map<int, std::map<std::string, int> > & local_names)
{
	return true;
}